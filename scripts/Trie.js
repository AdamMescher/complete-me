const completeMe = require('../scripts/CompleteMe')
const trieNode = require('../scripts/TrieNode')
const text = "/usr/share/dict/words"

class Trie {
  constructor(){
    this.length = 0;
  }
}

console.log(text);
